# Fivestacks

League of Legends draft ideas for five-player Flex teams, built with Svelte and Vite.

```bash
npm install
npm run dev
```

Pushes to `main` publish the container to GHCR for deployment at `fivestacks.agusbravo.dev`.
